name: Main — Test & Deploy

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  # ── 1. Tests ──────────────────────────────────────────────────────────────
  test:
    name: Unit tests & coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Tests con cobertura (mínimo 80%)
        run: npm run test:coverage

  # ── 2. Build & Deploy a Vercel (solo si tests pasan) ──────────────────────
  deploy:
    name: Build & Deploy a Vercel
    needs: test          # 🔒 solo corre si el job "test" fue exitoso
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Build de producción
        run: npm run build

      - name: Deploy a Vercel (producción)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: npx -y vercel@latest --prod --token "$VERCEL_TOKEN"
